{
    "compilerOptions": {
      /* 기본 옵션 */
      "target": "ES2022", // 코드를 어떤 버전의 JavaScript로 변환할지 지정합니다.
      "module": "NodeNext", // Node.js의 최신 ES 모듈 시스템을 따르도록 설정합니다.
      "moduleResolution": "NodeNext", // 모듈을 찾는 방식을 Node.js 최신 규칙에 맞춥니다.
      "esModuleInterop": true, // CommonJS 모듈을 ES 모듈 방식으로 가져올 수 있게 하여 호환성을 높입니다.
      "forceConsistentCasingInFileNames": true, // 파일 이름의 대소문자를 구분하도록 강제합니다.
  
      /* 엄격한 타입 검사 옵션 */
      "strict": false, // 모든 엄격한 타입 검사 옵션을 활성화합니다. (권장)
      "skipLibCheck": true, // 의존성 라이브러리(node_modules)의 타입 검사를 건너뛰어 컴파일 속도를 높입니다.
  
      /* 컴파일 출력 옵션 (tsc로 빌드할 경우) */
      "outDir": "./dist", // 컴파일된 JavaScript 파일이 저장될 폴더를 지정합니다.
      "rootDir": "./" // TypeScript 소스 파일의 루트 디렉토리를 지정합니다.
    },
    "include": ["**/*.ts"], // 컴파일할 파일의 범위를 지정합니다 (프로젝트 내 모든.ts 파일).
    "exclude": ["node_modules"] // 컴파일에서 제외할 폴더를 지정합니다.
  }